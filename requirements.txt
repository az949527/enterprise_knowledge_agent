# 企业知识库 Agent 依赖清单（统一单文件管理）
#
# 每行末尾 `#@ 节名` 标记所属环境，多个节用逗号分隔：
#   base    - 所有环境共享（文档解析 / LLM / 会话存储）
#   server  - 服务端 Web（FastAPI / Uvicorn）
#   ml      - 服务端 RAG 重模型（可选，体积大）
#   desktop - 桌面 EXE 运行时（Qt / PySide6）
#   build   - 打包工具（PyInstaller）
#
# 无标记的行默认归入 base。安装时用 scripts/requirements_sections.py 按节提取。

# ---- base：所有环境共享 ----
pydantic==2.9.2  #@ base
pydantic-settings==2.5.2  #@ base
PyMuPDF==1.24.11  #@ base
openai==1.45.0  #@ base
httpx==0.27.2  #@ base
openpyxl>=3.1,<4  #@ base
SQLAlchemy>=2.0.35,<3  #@ base
aiosqlite>=0.20,<1  #@ base
numpy>=1.22.0  #@ base

# ---- server：服务端 Web ----
fastapi==0.115.0  #@ server
uvicorn[standard]==0.30.6  #@ server
python-multipart==0.0.20  #@ server
aiofiles==24.1.0  #@ server
langchain-text-splitters==0.2.4  #@ server

# ---- ml：服务端 RAG 重模型（可选）----
sentence-transformers==3.0.1  #@ ml
faiss-cpu>=1.8.0  #@ ml

# ---- desktop：桌面 EXE 运行时 ----
PySide6-Essentials==6.6.3.1  #@ desktop

# ---- build：打包工具 ----
pyinstaller==6.10.0  #@ build
